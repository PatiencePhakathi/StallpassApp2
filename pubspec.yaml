name: stallpass_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  mobile_scanner: ^3.0.0  # For QR code scanning
  fluttertoast: ^8.2.2    # For showing toast messages
  provider: ^6.1.1        # For state management (optional but recommended)
  http: ^1.2.1            # <-- Added for API calls to backend

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

